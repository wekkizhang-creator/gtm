# 效率清单隐私政策

效率清单只在登录后展示当前账号的数据。任务、清单、日历订阅、专注记录、习惯、倒数日、便签、附件元数据、AI 配置和安全审计记录均按 `userId` 归属。

邮箱验证码通过配置的 SMTP 服务商发送。验证码只保存哈希和过期信息，不保存明文验证码。

AI Key 仅以混淆形式写入本地数据库，普通接口只返回脱敏展示值。AI 生成记录不保存完整 Key。

你可以导出账号数据，也可以在设置中申请注销账号。注销到期后，系统会删除账号业务数据并匿名化用户资料。
